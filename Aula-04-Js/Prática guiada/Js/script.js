// Prática guiada

// Passo 1

/* let nota= Number (prompt ("Informe uma nota"));

if (nota >= 90) {
    alert ("O conceito é A")
} else if (nota >=80) {
    alert ("O conceito é B")
} else {
    alert ("O conceito é C")
}

let dia=3; */

// Passo 2

/* let numero= Number (prompt ("Informe um número de 1 a 7"));

switch (numero) {
    case 1: alert ("Domingo")
    break;
    case 2: alert ("Segunda-feira")
    break;
    case 3: alert ("Terça-feira")
    break;
    case 4: alert ("Quarta-feira")
    break;
    case 5: alert ("Quinta-feira")
    break;
    case 6: alert ("Sexta-feira")
    break;
    case 7: alert ("Sábado")
    break;
    default: alert ("Dia inválido");
} */

// Passo 3

/* let temperatura= Number (prompt ("Infome uma temperatura"))

let temp= temperatura > 25 ? "Quente" : "Frio";
alert (temp); */

// Passo 4

/* let maiorIdade= prompt ("Informe true para maior de idade ou false para menor de idade") === "true";
let temCarteira=prompt ("Tem carteira? Digite true ou false") === "true";

if (maiorIdade && temCarteira) {
    alert ("Pode dirigir")
} else {
    alert ("Não pode dirigir")
} */

// Passo 5

/* let numero= Number (prompt ("Informe um número"));

if (numero >= 10 && numero <=20) {
    alert ("Dentro do intervalo numérico")
} else {
    alert ("Fora do intervalo numérico") 
} */

// Passo 6

/* let semaforo = prompt("Digite uma cor: Vermelho, Amarelo e Verde").toLowerCase()

switch (semaforo) {
    case "vermelho": alert ("Pare!")
    break;
    case "amarelo": alert ("Atenção!")
    break;
    case "verde": alert ("Siga!") 
    break;
    default: alert ("ERRO! Digite uma das cores novamente");
} */